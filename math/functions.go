package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Printf("Sqrt is %d", int(math.Sqrt(101)))
	fmt.Println(math.Pi)
	// "Capital first letters chars are exported and valid only \n", In Go, a name is exported if it begins with a capital letter.
}
